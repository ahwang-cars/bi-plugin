---
description: TODO — one sentence shown in /<plugin>:<command> autocomplete.
argument-hint: <arg1> [optional-arg2]
---

TODO — describe what this command does in 1-2 sentences.

## Args
- `$1` — TODO required arg.
- `$2` — TODO optional arg with default.

## Run

```bash
PY=$(${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.sh)
"$PY" "${CLAUDE_PLUGIN_ROOT}/scripts/your_script.py" --flag "$1"
```

## Output

TODO — what does success look like, what does failure look like, anything to surface specially.
